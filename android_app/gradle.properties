# Enable AndroidX and Jetifier for library compatibility
android.useAndroidX=true
android.enableJetifier=true

# Increase heap size for smoother builds
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
